rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Umožni čtení a zápis všem uživatelům v DEV prostředí
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
