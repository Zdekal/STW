// src/components/user/UserProjectRisks.js
import React from 'react';
import { Typography, Box } from '@mui/material';

export default function UserProjectRisks() {
    return (
        <Box p={4}>
            <Typography variant="h5" gutterBottom>Moje rizika projektů</Typography>
            <Typography variant="body2" color="text.secondary">
                Tato funkce bude brzy k dispozici.
            </Typography>
        </Box>
    );
}
